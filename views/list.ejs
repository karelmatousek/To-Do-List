<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>To do List</title>
    <link rel="stylesheet" href="CSS/styles.css" />
  </head>
  <body>
    <div class="box" id="heading">
      <h1><%=listTitle%></h1>
    </div>

    <div class="box">

      <% for(let index = 0; index < newListItems.length; index++ ) { %>
      <div class="item">
        <input type="checkbox" />
        <!--tímto řádkem se mi položky dávají do SAMOSTANÝCH BODŮ, ne na jeden řádek-->
        <p><%=newListItems[index]%></p>
        <!--tímto se vytvoří nový řádek-->
      </div>
        <% } %>
        
      <form class="item" action="/" method="post">
        <input type="text" name="newItem" placeholder="New Item" autocomplete="off"/>
        <button type="submit" name="list" value= <%= listTitle %>> + </button> 
        <!--pomocí console.log(req.body) zjitím, že když přidám soubor do /work - konzole to zaznamenná-->
      </form>
    </div>
  </body>
</html>
